{
  "name": "lambda-polls-manager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/app.ts --bundle --platform=node --target=node24 --outfile=dist/app.js --external:@aws-sdk/*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1030.0",
    "@aws-sdk/lib-dynamodb": "^3.1030.0"
  },
  "devDependencies": {
    "@types/node": "^18.19.130",
    "esbuild": "^0.28.0",
    "typescript": "^5.9.3"
  }
}
